# 整改实施记录

方案摘要：先建立可复现验证，再修复旧 Shell；新 Go SSH 客户端先作为候选程序，只有配置、程序和定时任务行为可以证明兼容才切换。普通升级不更换凭据、不搬迁密钥、不重启 Docker、不修改默认 Shell。发布和逐机迁移以证据为条件。

当前属于实施中，不能按本文件宣布计划完成。未发布新 stable，未安装 Go 候选到个人环境，未连接 Gist 内机器。逐机名单及维护窗口尚未提供，个人迁移状态为“未纳入范围”，不是“迁移成功”。

## 阶段交付与剩余工作

| 阶段 | 已落地 | 尚需完成 |
|---|---|---|
| A 基线 | 历史失败样本、统一 Make 入口、Actions 汇总门禁、QEMU 驱动及镜像摘要 | 组件映射已落地但系统场景仍较粗；Ubuntu/Debian 固定包源已验证；OpenWrt 固定签名包源已有 TCG 与托管 KVM 证据；全场景允许变更清单、门禁变异验证 |
| B 高风险修复 | 代理结果、Squid 解析/双文件恢复、sudoers 候选校验、Node 错误传播、Python 执行边界、Zsh 托管与插件保留 | 完整入口故障复现、并发与中断、SSH 旧客户端全字段验证 |
| C 迁移基础 | Go 同目录提交、操作记录、后续修改冲突保护、真实第二份文件失败恢复 | 检查器已覆盖部分 Shell 操作；全部历史发现、ACL/中断恢复仍待补 |
| D Go SSH | YAML 规范化、路由、独立配置/续签、CLI 初版、受限 meta.env 解析 | Linux 旧任务的完整迁移/触发/卸载已通过；macOS 专用账户与 GUI 采纳已通过原生验证；原生任务回滚已验证；完整旧客户端发布升级待补 |
| E 长期整改 | CA 自定义位置记录、Zsh 保守清理、HUD 共享预算、规则简化、本地示例绑定 | HUD repair/设置 CLI、XDG、访问公钥、Host 片段已实现并有部分入口验证；组/备份和各工具完整恢复矩阵待补 |
| F 发布 | 冻结源码构建、四平台归档、原生暂存/渲染、拒绝不完整证据的门禁 | 全套候选验证、产物升级/回退、公开安装、stable 提升 |
| G 个人迁移 | 无生产改动 | 明确机器范围，离线检查，逐机应用与真实登录验收 |

## 已有用户影响

- Zsh：不删除既有 OMZ 或未知插件，不改默认 Shell；--cleanup-all 现在保守地只清理托管块，并提示组件保留。损坏标记拒绝写入。需要核对这个有意收紧的卸载契约。
- Squid：普通重跑保留 /etc/squid/passwd；改密必须显式 --rotate-credentials。配置变化仍涉及服务重启，应安排窗口。恢复范围是配置、认证文件和服务状态，不承诺撤销 apt 的包安装。
- Python：新装默认 uv，其他组件明确选择；不再批量改 Poetry/PDM 偏好。现有工具保留。首次依赖安装与全部上游版本验证尚未完成。
- Docker 代理：重启默认否，重启失败返回失败。现有 daemon 不因默认选择重启；文件事务已接入；真实 daemon 的本地代理拉取与权限生命周期已通过 Ubuntu 全系统验证。
- SSH：现有 Shell 下载入口保留。Go 候选没有通过旧任务采纳验收前不切换；未知旧任务、手改配置返回冲突。可从关联旧证书验证并采纳 CA 指纹；无法证明关联时要求明确处理，不自动信任新 CA。
- CA：自定义目录/名称以非执行数据保存至 ~/.lazycat/ssh-ca-location；不搬迁、不重建已有 CA。历史自定义位置不会凭空被发现，需要明确采纳入口。
- HUD：总处理预算 4.5 秒、锁最多等 4 秒，请求受剩余预算约束。送达状态不明会保留标记以阻止自动 Stop 重发；可能少一条通知，不能承诺严格一次送达。

## 42 项追踪

状态表示当前实施程度；“回归通过”只对对应测试有效。机器可读版本见 remediation-status.json。已验收的规则/文档调整与明确不实施项逐项标记；其余未完成项继续阻止发布，不能把部分实现当作最终验收。

| 编号 | 审阅问题 | 状态 |
|---|---|---|
| 1 | SSH 清单的数据可以变成客户端执行指令 | 部分实现，验收未完成 |
| 2 | SSH CA 自动签发没有形成服务端授权边界 | 按用户确认的范围不实施团队授权服务 |
| 3 | 默认 CA 路径无法按设计展开 | 原审阅结论被真实签发与登录反证，不按已确认缺陷整改 |
| 4 | 代理测试失败后，“是否继续”的逻辑不可达 | 对应审阅缺陷已修复并验证 |
| 5 | HTTP 代理测试实际上可能走了直连 | 对应审阅缺陷已修复并验证 |
| 6 | Squid 把日志中有没有 ERROR 当作退出状态 | 对应审阅缺陷已修复并验证 |
| 7 | 多个安装步骤失败后仍可能返回成功 | 部分实现，验收未完成 |
| 8 | Python 执行封装混合了数据和 Shell 代码 | 对应审阅缺陷已修复并验证 |
| 9 | Zsh 识别不了自己写进去的加载语句 | 对应审阅缺陷已修复并验证 |
| 10 | 一句说明文字实际执行了 p10k | 对应审阅缺陷已修复并验证 |
| 11 | SSH Host 覆盖逻辑不兼容 macOS awk | 对应审阅缺陷已通过保守片段策略整改 |
| 12 | 标记块损坏时会删除后续用户配置 | 对应审阅缺陷已修复并验证 |
| 13 | 系统配置存在“先生效、后校验”的窗口 | 部分实现，验收未完成 |
| 14 | 临时文件替换会丢失原文件属性 | 部分实现，验收未完成 |
| 15 | SSH 私钥的默认工作流反向了 | 对应审阅设计问题已整改并验证 |
| 16 | SSH 客户端已超出 Bash 擅长的复杂度 | 部分实现，验收未完成 |
| 17 | YAML 版本字段没有承担版本契约 | 对应审阅缺陷已修复并验证 |
| 18 | yq 依赖只检查同名命令，未检查实现 | 对应审阅缺陷已修复并验证 |
| 19 | 配置同步被 CA 可用性绑架 | 对应审阅缺陷已修复并通过真实联调验证 |
| 20 | 普通运行隐式承担安装和升级 | 部分实现，验收未完成 |
| 21 | SSH 状态分散，卸载承诺大于实际行为 | 部分实现，验收未完成 |
| 22 | CA 自定义设置只存在于本次进程 | 对应审阅缺陷已修复并验证 |
| 23 | 自动续期缺少与证书生命周期对应的策略 | 部分实现，验收未完成 |
| 24 | 新节点实现已有回滚，但安装和移除不对称 | 部分实现，验收未完成 |
| 25 | Zsh 清理通过“长得像”判断所有权 | 对应审阅缺陷已修复并验证 |
| 26 | “一键 Python 环境”捆绑了互相重叠的选择 | 对应审阅设计问题已整改并验证 |
| 27 | 权限范围经常大于实际业务范围 | 部分实现，验收未完成 |
| 28 | 备份格式没有形成恢复协议 | 部分实现，验收未完成 |
| 29 | “优雅”被错误地绑定到了动态最新版 | 规则整改已验收 |
| 30 | “不要藏错”不该写成禁止捕获错误 | 规则整改已验收 |
| 31 | 规则把具体实现手法提升成普遍原则 | 规则整改已验收 |
| 32 | 测试规则包含已被复现推翻的经验结论 | 规则整改已验收 |
| 33 | 最严格的测试要求没有落实到风险最高的代码 | 分层自动门禁及失败证据机制已验收 |
| 34 | 测试环境没有和支持范围一一对应 | 平台验证等级和支持边界已明确并验证 |
| 35 | 文档维护靠复制，容易产生事实漂移 | 部分实现，验收未完成 |
| 36 | 交互界面占据核心控制流，脚本难以组合 | 部分实现，验收未完成 |
| 37 | 面向本地开发的命令默认暴露到所有网卡 | 文档示例整改已验收 |
| 38 | 安装记录过度依赖托管文本完全相等 | 对应审阅缺陷已修复并验证 |
| 39 | 通知锁等待预算与网络耗时不匹配 | 对应审阅风险已修复并验证 |
| 40 | 通知产品策略过多依赖模型遵守全局自然语言 | 对应审阅缺陷已修复并验证 |
| 41 | 部分 HUD 设置只能通过菜单或手改 TOML | 对应审阅缺陷已修复并验证 |
| 42 | 发布流程值得保留，但验证与发布可以更清晰地分开 | 部分实现，验收未完成 |

复现入口与验证边界见 [TESTING.md](TESTING.md)。完整用户计划仍是最终验收依据，本记录不缩减其范围。

## 最近验证记录（2026-09-08）

`make check test`：Shell 语法、ShellCheck error 级、文档文件链接、actionlint、Go vet/gofmt、43 个 Python 用例及两模块 Go race 通过。后续新系统场景仍须独立执行，不沿用这次结果。

Ubuntu QEMU 已实际观察旧 systemd 任务采纳后的定时触发，并确认未替换仍有效证书；本次场景最后的 `loginctl show-user` 因用户管理器退出而失败，整次结果保留为失败。随后在全新 overlay 完成整场复测（20260907T175508.388474Z），包括真实 Docker 代理请求/权限生命周期和 linger 检查，结果通过。前次失败日志仍保留。Debian 软件源初始化超时，记录为环境失败。

候选 `release-candidate-8` 的 macOS ARM64 原生与断网 Linux ARM64 容器实际归档暂存/渲染及 Shell 包语法/只读检查通过，此后四平台均已通过远程原生产物检查（见下方记录）。开发候选不可用于 stable。完整机器迁移报告仍为未纳入范围；没有访问个人服务器或迁移本机安装。

Ubuntu Docker 场景已验证 daemon 实际经过本地代理、拒绝时拉取失败、放行后从本地 TLS 仓库拉取并检查镜像，以及普通配置不重启、显式重启、代理移除和组权限撤销。它不代表 Docker Hub 网络或任意企业代理已验证。

下一条固定输入验证使用与基础镜像摘要绑定的 apt 包锁。包准备在独立干净 VM 仅下载、不安装，导出每个官方包的 URL、版本元数据和 SHA-256；产品场景从另一份新 overlay 开始，QEMU 限制外网，真实 apt 只使用校验后的本地源。此链路已用两个独立 overlay 通过完整生命周期；第二轮结果目录为 20260907T181453.710039Z。Debian/OpenWrt 离线源尚未完成。

## 首轮远程 Actions

草稿 PR #8 的首轮 reliability（run 34151819016）中，quality、macOS 行为、Debian 和 OpenWrt 全系统作业通过；Linux 容器源码目录权限与 Ubuntu 启动探测导致两个环境失败，required-checks 正确失败，未把遗漏当成通过。HUD 独立测试/构建工作流通过，没有执行发布作业。

已针对日志修正临时源码目录的可遍历权限和有界启动轮询，并接入四平台实际候选产物检查；这些后续工作流变更需在下一轮远程验证。Debian 的成功不覆盖本地失败，也不代表 Debian 已有离线固定包源。

第二轮 reliability（run 34152445967）中，Ubuntu/Debian 系统、Linux/macOS 行为和四平台候选产物全部通过。OpenWrt 在 KVM 下暴露 procd 状态先于监听端口就绪的竞争；已有修改和受控延迟/超时用例，等待下一轮真实验证。未重跑原失败来覆盖记录。

公共 Go rollback 已收紧：任务生命周期恢复未完成前，包含原生任务的记录拒绝自动回滚，避免只恢复文件却报告服务也恢复了。

第三轮 reliability（[run 34153115082](https://github.com/KroMiose/LazyCat-Scripts/actions/runs/34153115082)，提交 b8b9e7d）全部必需作业通过：Ubuntu、Debian、OpenWrt 完整系统，Linux/macOS 行为，以及 Linux/macOS × AMD64/ARM64 四平台实际候选包。OpenWrt KVM 监听竞争修复通过真实系统验证。HUD run 34153114908 测试/构建通过，没有发布。weekly-full 在 PR 事件不要求执行，其跳过不能视为每周完整覆盖已经通过。

正在增加 systemd 任务更新的六种状态组合（持久启用/临时启用/禁用 × 运行/停止），防止修改间隔隐式重新启用用户停用的任务。适配测试覆盖管理器故障，真实系统结果另行记录；macOS 生命周期不沿用此 Linux 结论。公共回滚的原生任务检查移入事务锁内，避免检查与实际读取不同记录。

Ubuntu 真实上游安装（20260907T185122.590682Z，提交 b8b9e7d）完整通过：新用户无 nvm/uv 起点，安装 Node 22.20.0 与 uv 0.10.0，实际运行 Node 并创建/运行 Python 虚拟环境。此场景预装了明确的系统依赖，不代表缺失 curl/git 时自动安装依赖已验证。

事务回归用同一测试文件对比提交 3d70f5f 与修改后实现：旧实现稳定失败于“SIGKILL 后重跑覆盖未完成事务”和“rename 后报错遗漏当前文件恢复”；修改后 Go race 通过。原始失败日志在本地 artifacts/regression-proof/transaction-3d70f5f-supported-extract/before.log。新实现对同资源的未完成事务返回冲突，允许显式 rollback 后重跑；回滚不覆盖后来用户编辑，doctor 报告损坏记录。文件 rename/remove 后同步父目录。该证据不代表全部原生服务或 Shell SIGKILL 恢复已完成。

第四轮 reliability（[run 34153832563](https://github.com/KroMiose/LazyCat-Scripts/actions/runs/34153832563)，提交 3d70f5f）全部必需作业通过。Ubuntu 与 Debian 的真实 systemd 均验证六种启用/运行组合在更新间隔后保持不变；四平台候选归档再次通过原生检查。这轮尚未包含后续事务中断修复。

第五轮 reliability（[run 34154254968](https://github.com/KroMiose/LazyCat-Scripts/actions/runs/34154254968)，提交 e8a7fe1）及 HUD run 34154254861 均通过。此轮包含 Go 中断事务修复；后续 Shell 锁恢复和包源准备调整尚未包含。

Shell 增加显式 `lazycat-check.sh recover-lock <目标绝对路径>`：仅当原 PID 确认不存在时归档旧锁，不改目标或备份；活跃 PID、损坏锁和恢复竞争拒绝处理。真实 SIGKILL 注入到 Shell 文件替换后，随后恢复锁与回滚，验证原内容、权限和恢复前快照。该行为已本地验证，不能代替全部系统工具的服务恢复。

Debian 包锁导出首轮因无关翻译索引超时失败（20260907T190111.174025Z）；第二轮去掉翻译后进入大包下载，但在发现 `mirror+file:` 无法供外部驱动复现后主动中止（20260907T190731.316713Z）。两轮均不算通过。新准备流程由真实 apt 输出经过认证的包元数据，主机按 SHA-256 下载；正在独立环境验证。

包源准备已补充 Debian epoch 文件名与无 MD5 的安全更新索引样本，SHA-256 仍为必须项。大包下载发现仅用 socket 空闲超时无法约束持续慢响应，改用 curl 的 180 秒总时限、大小限制及下载后摘要校验；本地真实 HTTP 截断测试确认失败不会进入缓存。旧下载诊断主动中止，保留环境失败记录，未作为通过。

主分支原先没有保护规则；现已启用 `required-checks`（限定 GitHub Actions app 15368）、要求分支与 main 同步，并对管理员生效。此修改会影响合并和直接推送。工作流尚在本草稿中，基于旧 main 的其他 PR 在带入新工作流前会等待必需检查；主分支定期调度也要等工作流合入后才开始。

第六轮 reliability（run 34155373821，提交 846761d）与 HUD run 34155373532 均通过，包含 Shell SIGKILL 后锁恢复。Debian 主机下载与一次诊断重试均在 git 包下载阶段超时，保留失败记录；不继续盲目重跑，转由 GitHub 托管 runner 的 environment-locks 工作流准备候选包锁，再做独立离线生命周期验证。准备成功本身不算产品测试通过。

托管 runner 的 environment-locks（run 34156222825）已完成 Ubuntu/Debian 两套元数据解析和包摘要验证。Debian 候选锁 55 包、106146924 字节，锁摘要 ec31cc9694f32d7b656b5d45a7ce7b6be8bd4f0206014f927301cede209f4714；来自 PR 合并测试提交 429f32e3a5c5cdeb304f84972cab91f9685db36b（包含分支提交 10876a5）。已接入新的 Debian 断网 Docker/核心生命周期作业，待实际验证，不将环境准备成功当作产品通过。

macOS 候选增加实际账户 HOME 校验、当前 launchd 域选择及已加载旧任务的域保留。只采纳历史 Shell 精确生成的 plist；保留间隔、PATH、HOME、RunAtLoad 与日志路径，仅增加 --scheduled。未知、自定义、未加载而无法确认原域的旧任务保持冲突。对应专用账户 native runner 场景验证真实触发、停用偏好、旧任务采纳和冲突；原生结果尚待取得。这仍不是完整旧 Shell 客户端与依赖升级验证。

Debian 固定包源已通过全新断网 KVM 的完整 Docker/SSH/Squid/sudo/续签任务生命周期（run 34157169780，合并测试提交 08faf111ef53b0483a28823701995712adbeaf7d，包锁摘要 ec31cc9694f32d7b656b5d45a7ce7b6be8bd4f0206014f927301cede209f4714）。此轮总体仍失败：macOS 专用新账户返回 `disabled services = (no disabled services)`，客户端原先不识别而安全拒绝。run 34157530082 增补原始输出并确认两架构相同，账户清理也改为查询真实目录记录；失败未覆盖。现已增加该明确格式的解析与回归。

VM 驱动增加只读 bind mount 与 root 写入拒绝探测；源码和离线软件源在测试期间不可写，OpenWrt 重启后重新建立只读挂载。驱动 SSH 只使用生成的测试密钥，明确禁用继承的 agent。此隔离加固等待下一轮完整系统验证。


run 34157931567 与 34158692299 的 Ubuntu/Debian/OpenWrt 系统作业通过，包含源码只读挂载与 SSH agent 隔离；总体仍因 macOS 原生注册失败而失败。后者独立最小任务证明：新账户 user/<uid> 域默认 plist 返回 5，显式 Background 会话任务返回 0。新任务据此声明 Background，已有 GUI/历史任务保持设置；更新间隔也保留历史 PATH、RunAtLoad 和日志。后台新账户与 runner GUI 旧任务采纳分开记录，修复后原生结果待取得，不计为通过。


run 34159367995 的 macOS Background 任务首次安装和真实间隔触发已通过，随后在保持停用偏好的场景失败，整轮仍不算通过。原生 launchctl 输出使用 enabled/disabled 字样，解析器先前只接受 true/false；已补充明确格式、拒绝未知状态及原生断言。GUI 历史迁移尚未执行，不记为通过。


run 34159699770（合并测试提交 ca97cb9de9df32460707c3ba04cc1cedc4391329，包含分支提交 5d05115）的 macOS 15 ARM64/AMD64 原生作业均通过：专用标准账户真实定时触发、重复安装、未加载/停用偏好保持与移除；runner 实际 GUI 会话中的历史任务采纳、间隔更新保留偏好、手改冲突和密钥保留。两种身份的清理均通过。它验证历史任务定义，尚未验证完整旧 Shell 客户端与依赖升级，也未完成公共任务状态 rollback。

相同 launchd 状态回归对 e217bd0 因“不识别 disabled”断言失败，对新实现通过；本地记录 artifacts/regression-proof/launchd-enabled-e217bd0。驱动的两次归档准备错误单独记录，不计入产品旧失败证据。本地 47 个 Python 测试通过，发布门禁另有遗漏场景、降低执行级别、平台替换、缺断言、日志篡改和产物缺失的拒绝验证。后续 macOS 报告补充逐场景未运行状态、JUnit 和 Job Summary，原生执行范围不因此扩大。


OpenWrt environment-locks 首轮 run 34160343129 因导出压缩索引后直接验签失败，保留为环境失败。run 34160633034 确认 opkg 缓存均为 gzip，按原内容解压后官方 usign 验签及 download-only 通过，目标包未安装。27 资源（13 IPK＋7 组原始索引/签名）、9349309 字节，初始候选锁摘要 68ec2f74d03f9fe5cb74cca52ecb89e47d22251b07ee6c78849f75e598ad1501。

独立本地 TCG 全系统 20260907T204836.402692Z（250fa6d）使用该锁、QEMU restrict=on、只读源码与包源，实际 opkg/procd、证书正反登录和重启持久化全部通过。随后将原始签名元数据压缩冻结在仓库，避免上游可变索引破坏固定输入验证；原始内容/包摘要不变。采用元数据 fixture 的锁摘要为 834bcf3d1d7ec24d8687fccc5504d18d562ea01b18911309952b759cc56b9908，PR/每周检查改用此锁，另保留真实上游 opkg 验证。托管同锁结果尚待取得，不宣称所有 OpenWrt 故障场景或实机覆盖已经完成。

CLI 新增参数值在写入前校验：错误续签间隔、指纹、回滚路径和配置 URL 返回 2，HOME 无新增文件。相同实际入口测试在 eaf7075 对错误退出码失败，新实现通过。更换配置来源保留已明确采纳的 CA 指纹；不会因换文件/URL 悄悄清除信任，损坏原状态或并发编辑则停止。


run 34161071064 的 OpenWrt 全系统作业通过，合并测试提交 9bdee717840db3f76e9ae3486280be00c7eb8b0c（含 c67f0eb），使用冻结签名元数据后的锁 834bcf3d1d7ec24d8687fccc5504d18d562ea01b18911309952b759cc56b9908。托管 KVM 再次验证断网真实 opkg/procd/SSH 及重启，源码和包源只读。尚未因此扩大到 ImmortalWrt 实机或全部故障场景。

事务锁新增父目录符号链接检查，避免拒绝文件写入之前先在未托管目标创建锁目录。实际文件系统回归对旧 c67f0eb 失败、新实现通过，记录 artifacts/regression-proof/lock-symlink-c67f0eb；此项不等于所有文件路径竞争与原生任务事务已完成。


run 34161071064 的全部必需作业最终通过；它不包含后续符号链接锁检查，且 PR 事件不执行 weekly-full。另在本地暂时替换发布场景校验函数为空操作，既有测试正确报告 ValueError 未触发；恢复实现后通过，记录 artifacts/mutation/release-scenario-contract。这是门禁的受控变异验证，不是产品场景覆盖。


首次完整工作流 run 34160052011（449029d）最终通过：三套系统各两个独立生命周期、Ubuntu/Debian 重新下载镜像和安装包后重建环境、真实上游 Node/Python 安装与解释器行为、Ubuntu 实际 Docker 上游安装，以及同时执行的四平台候选/原生任务检查。它早于 OpenWrt 固定源与后续修复，只证明当时已登记的范围；并非最终计划全覆盖。

第二次完整工作流 run 34161905507（4f5080d）的四条 weekly-full 线路均通过，新增 OpenWrt 固定签名包源的两份 overlay 与真实软件源，以及 Debian 真实 Docker 上游；整体仍失败，因为 Intel runner 没有 hw.optional.arm64 这个 sysctl OID。之前 run 34161687184 已保留相同原生观察，后续 PR 推送取消了那轮剩余作业，不能算绿色。修复只接受 Intel 上已观察的精确 unknown-oid 响应，其他未知/转译架构拒绝；run 34162914509 的四个平台候选作业已通过这一检查。

旧 Shell 续签现在有锁定 yq v4.53.2 的真实入口场景；该依赖只用于测试，不加入 Go 客户端运行依赖。run 34162914509 的 Debian 新场景在执行原始旧脚本时发现打印成功后 RETURN trap 的 tmp_yaml 已失效，导致原脚本最终返回失败。原日志保留；测试现明确记录此旧缺陷，再验证修复后的路径，尚待结果。改动包括唯一证书候选、EXIT 作用域清理、关联公钥/证书格式检查、远端临时目录清理、保留已有合法权限、不覆盖部分密钥、不跟随证书/密钥符号链接，以及禁止续签自动学习 known_hosts。完整 CA/principals/有效期严格验证主要由 Go 实现；此 Shell 场景不代表完整历史发布升级或所有文件事务已完成。


run 34163524342 全部必需作业通过，包含 Ubuntu/Debian 真实旧客户端续签入口；Debian 合并测试提交 c7762cca9ce9e7c6d4a995ec95738e915e0e066c（含 afd09ac）验证修复后默认 CA 路径签发/新连接、.tmp 用户文件、400 权限密钥/证书、750 目录、故障保留与再次执行、部分密钥及符号链接拒绝。随后增加 c8ffb57 原始客户端/库的默认路径失败对照，尚待该新增场景结果；不把已修复路径的成功冒充原始版本失败已执行。

## 原版默认 CA 路径审阅纠正

run 34164160269（bdc5077，合并测试提交 c9034304c3e33517242d82e261ef98057f9c0c6b）整体失败，required-checks 正确阻止通过。Debian 原始 c8ffb57 客户端和库通过默认路径完成真实签发，随后才因 RETURN trap 的 tmp_yaml 未绑定退出。原先“Shell 引号阻止 ~ 展开，所以 CA 路径必然失败”的审阅忽略了 ssh-keygen 自身的路径展开，应撤回；不能把后续非零退出误作路径故障证据。

保留该失败的完整 artifact。修正场景检查原始证书的签发 identity 和真实新连接，并单独断言已知清理错误；run 34164951210 的 Ubuntu/Debian KVM 场景均通过。已有默认路径处理不据此宣传为确定性缺陷修复。此纠正不替代其他路径、并发与权限验收。

## 卸载冲突与任务保留

客户端卸载现在先检查生成配置、程序和相关资源归属，再处理续签任务；带任务的卸载将客户端与任务文件放入同一文件事务。原先先卸载任务、再发现客户端手改的顺序会让拒绝卸载仍影响续签。相同实际 CLI 测试在 282152e 返回错误的任务元数据错误（退出 1），新实现优先返回客户端归属冲突（退出 3）并保留文件；隔离的无效任务元数据保证该定向测试不会调用本机服务管理器。测试准备工具的 Python 兼容失败单独保留。

run 34164951210 的 Ubuntu/Debian KVM 新系统场景已通过：真实 active/enabled 任务在两种手改冲突后保持运行，完整卸载使用统一操作记录，删除后 systemd LoadState=not-found。文件事务合并不等于已完成 SIGKILL 后原生服务状态恢复，公共任务 rollback 仍保守拒绝。

截至 8c5e029 共有 7 项最终处置（含第 3 项按反证结案），35 项未达到最终验收。这是当时的状态，后续逐项处置见下方记录及上方表格。

## 完整验证通过：8c5e029

手动完整工作流 [34164951210](https://github.com/KroMiose/LazyCat-Scripts/actions/runs/34164951210) 在提交 8c5e0293d73a1127a4ac9c2d146f01d67c2e744f 上全部通过。包含 Ubuntu/Debian 固定镜像与包源的两份独立 overlay、第一轮无缓存下载、两平台真实 Node/Python 和 Docker 上游、OpenWrt 两轮固定签名包源及真实 opkg/重启、随机顺序与有界 fuzz，以及四平台原生产物和 macOS 任务。该结果只覆盖已经实现的场景，不能替代尚未实现的完整发布契约。

同代码的 PR 工作流 [34164941301](https://github.com/KroMiose/LazyCat-Scripts/actions/runs/34164941301) 也全部通过。后续验收记录更新是文档变更；这里保留实际执行的代码提交，不将后续提交冒充已经运行过完整验证。

本地本轮 make check/test 包含 51 个 Python 用例及 SSH/HUD Go race，通过；卸载改动后另行运行 make check test-migration，通过。Ubuntu 与 Debian artifact 已确认 status=passed、acceleration=kvm、dirty_worktree=false。原始错误断言、旧实现失败和因后续提交取消的运行继续保留，不覆盖为成功。


## 当前原生回滚与历史入口验证

e6f949c 的 run 34175119459 全部 PR 必需作业通过。任务更新、迁移和卸载在暂停前写持久日志，公共回滚恢复文件及原 systemd/launchd 状态；后续用户文件或任务启用偏好变化会阻止覆盖。真实 Linux 暂停中 SIGKILL、六种状态组合回滚、卸载回滚及 macOS 原 domain/disabled 恢复均通过。上文早期“公共任务 rollback 尚未实现”只描述当时提交，不是当前状态。

Squid 原始完整入口在真实 parser 输出 FATAL 而没有 ERROR 时误报成功；新入口拒绝提交。真实停止服务后注入 restart 失败，以及监听检测超时，均验证配置/密码共同恢复和旧凭据继续可用。首次 readiness 观察失败 run 34174135767 保留，修复后的 run 34174565071、34175119459 通过。

21b7fdf 增加代理、Zsh、yq 的完整旧/新入口对照。旧代理失败后无法继续、HTTP 实际直连；旧 Zsh 重复加载、覆盖插件、执行说明中的反引号以及损坏块清理吞掉用户尾部内容，均有实际入口断言。yq 错误实现用例验证新版在请求前停止且文件状态不变。原生 CI 结果确认后逐项更新验收。

仓库查询仅发现 HUD v0.1.0/v0.1.1 历史正式资产，没有旧 SSH/Shell 正式发布包。SSH 的历史源码/任务样本不能标成“旧正式包升级”。HUD 新增真实 v0.1.1 二进制数据升级与回退用例；不包含发布安装器中断恢复。没有改动个人安装或正式 stable。

d189c3b 的 run 34176125168 已通过 Linux/macOS 行为作业，包含真实 HUD v0.1.1 发布二进制的升级采纳、重复运行、二进制回退和卸载。当前逐项处置增加至 15 项，27 项仍阻止发布。这是对应审阅缺陷的结案，不代表工具的全部生命周期矩阵已覆盖。文档表格与 JSON 状态现在由 quality 检查保持一致。

Node 新发现并修复了实际运行未验证的问题：nvm install 返回成功之后，即使 node/npm 已损坏，旧入口仍打印完成。新入口先检查 nvm、实际执行 Node 小程序和 npm，再确认成功、处理默认版本；未收到有效选择时返回失败。故障注入在 d189c3b 上复现误报，新实现通过；原始失败保留在 artifacts/regression-proof/runtime-d189c3b/before.log。Python 存量工具、带引号和中文 HOME、项目 .venv 与 Poetry keyring 偏好保留用例也通过。本地共 58 项 Python 用例通过；真实上游尚须验证此次 Node 修改。


## 启动限流恢复与本轮补充验证

完整运行 34176462302（c935a59）的 Ubuntu 上游线路安装 Node/Python 成功后，在重复安装续签任务时触发真实 systemd start-limit-hit；产品未能撤回自己新建的失败任务，整体失败。原始 test.log/state.log 保留，不能归为网络故障。新实现仅在操作日志和文件归属均匹配时清除本次新建任务的失败状态；已有失败任务及其他服务不会被重置，安装仍返回失败。

新增夹具在 34178623052、34178662431 初始化时遇到 systemd 已卸载的空任务对象。0ee75f7 仅接受该已观察的 unloaded-unit 响应，其他初始化失败仍阻止通过并归为环境错误。修复后 34179540092 的全部 PR 必需作业通过；Ubuntu/Debian test.log 明确记录真实限流失败、恢复任务缺失、保留凭据和显式重装通过。

旧 Shell 客户端新增启动前 root 拒绝：保持原有用户身份要求，但不再先执行用户可写缓存库。断网只读源码容器中，原始入口执行了缓存观察者，新入口退出1且未执行缓存、未创建 SSH 状态。本地 62 项行为测试和 make check 通过。source/trust-ca 更新复用同一份源配置观察，首次配置也保护并发新建的 meta.env。

第38、40、41项按实际原生入口证据结案，当前18项有最终处置，24项未完成；每项结案不替代全部工具生命周期和发布门槛。第39项继续增加慢503与并发等待者场景；Stop Hook 按既有非阻断契约退出0并写stderr，测试同时断言错误内容和真实HTTP请求，不将退出0当作发送成功。

续签增加密钥、证书及源配置的网络请求前快照，提交时检查同一观察；用户在签发期间改动文件时返回冲突，不替换证书。真实 CLI 配合 ssh-keygen 生成证书、仅替换 SSH 传输边界的测试，覆盖三类并发凭据修改、源修改、正确签名、错误 CA/密钥/principals/类型和有效期。e3b8a66 在三类并发修改及损坏超大检查间隔时错误返回0的日志均已保存。新实现还拒绝损坏任务记录和溢出的间隔，按三分之一、两个检查间隔及一半上限验证续签窗口；SSH Go race 已通过。

Linux 扩展属性丢失在真实 XDG 修改入口复现（artifacts/linux-files/20260908T023927.666199Z）。GNU cp -p 未保留 user xattr；现在显式要求复制 mode/ownership/timestamps/xattr，复制失败停止提交。Linux 需要支持这些参数的 GNU cp，macOS 保持系统 cp -p。修复后容器中的实际改写、重复运行和公共文件回滚均保留扩展属性（20260908T024022.459685Z）。文件回滚不撤销 XDG 已明确执行的目录搬迁；ACL及扩展属性的并发变化检测仍未完成，不据此将第14/28项结案。

368e993 的 PR 34180916575 中，Linux/macOS 行为、HUD 慢503等待者和四平台归档通过，但 Ubuntu/Debian 系统测试在旧任务刚启动时立即执行迁移，被客户端正确拒绝。该旧任务使用 OnBootSec，enable 并不等待续签完成；原日志中的“legacy renewal is still running”保留。夹具改为清除自身旧状态记录、等待一次真实续签产生记录并结束、确认 Result=success 后再迁移，设置180秒截止时间而非固定等待。

Python 旧完整入口的断网容器对照，使用真实目标 UID 与明确的 sudo 登录环境/apt/curl 适配替身：旧版在 apt/download 失败后仍显示完成，裸 export 输出虚构变量，并试图修改 keyring；新版保留工具和用户 shell 配置，不执行隐式安装、不输出环境。首次夹具 /tmp 与 HOME 的 noexec 失败分别保留在 20260908T024401.113884Z、20260908T024444.880266Z；补执行能力基线后 20260908T024539.960215Z 通过。这不是实际 sudo 登录环境或依赖首次安装验证。

SSH README 的三个下载示例及 CA 菜单生成的节点命令，改为唯一临时文件、下载失败即退出、EXIT 清理。CA 公钥通过 Bash 参数转义传递，注释中的命令替换/反引号不再成为命令。实际文档片段及真实菜单输出的隔离测试覆盖下载失败、执行失败、清理和公钥字面值保留；HTTP 下载和 sudo 用明确的替身，不能标为公开安装通过。有效的镜像 LazyCat 别名保留。

第39项按368e993的两平台原生通知作业证据结案，当前19项有最终处置、23项未完成。


完整工作流34180343719在e3b8a66上全部通过，包含已登记的固定环境重建、重复overlay、真实Node/Python/Docker上游和OpenWrt完整线路。它早于后续证书与扩展属性修复，不能代替当前候选版本验证。188da91的PR34181587057也全部通过，包含旧任务完成后再迁移、Python历史入口对照以及文档命令验证。368e993的手动完整运行34181082409在待执行时取消：已有PR证明夹具时序缺陷，未把该取消记录标成通过。

第8项按真实Linux容器证据结案，当前20项有最终处置、22项未完成。Node --check 同样新增root检查前置，防止只读检查先执行用户nvm代码；原368e993样本与新入口在断网root容器中对照通过。CA自定义目录/名称新增旧/新完整菜单跨进程对照，原版遗忘设置、新版读取原密钥，不重新初始化。

Python用例清单由tests/python-cases.json逐条登记，quality及行为运行器检查缺失/新增/重复ID。更新清单需显式执行python3 tests/inventory.py --write并审阅差异；不能用重新生成清单代替审查被删除用例。受控变异加入一个不存在的必需用例后，66个实际用例虽然通过，运行器仍退出1，JSON/JUnit/摘要都记录漏测（artifacts/mutation/python-inventory）。恢复清单后通过。此清单仅覆盖Python用例，Go及独立原生/VM场景继续分别报告，不据此声称全矩阵一致性已完成。

证书补充校验拒绝“起始时间很早、剩余时间短但总有效期超长”以及用重复principal掩盖缺失身份；368e993实际CLI/真实签名样本错误返回0的日志保留。新实现通过SSH Go race与签名入口回归，既有密钥、CA指纹及正常有效期契约不变。

CA位置记录改用同一套Shell文件事务，公共检查器增加~/.lazycat记录发现。真实入口验证第二套CA位置切换后可回滚指向第一套，两套密钥都保留；记录提交期间的用户编辑触发冲突。初始化已生成的密钥不会因位置记录失败而自动删除，仍需审阅后恢复位置。Linux系统场景新增实际CA脚本init/show/重复拒绝及菜单签发，并用真实SSH连接验证错误CA、过期和无证书被拒绝；该新增场景待当前提交CI验证。

免密sudo入口现在验证实际普通用户身份和可安全表达的既有文件名，未知/无法表示的账户返回2，不再生成被sudo忽略或可能跨路径的文件。带点号等非旧格式用户名暂不自动迁移，现有权限保持不变。启用及移除只接受完整匹配历史托管规则的文件；额外手改或符号链接返回冲突，不覆盖、不撤权。新的VM场景对照原始入口删除手改规则和新版保留权限，随后验证显式移除；真实结果仍待CI，不将静态检查作为权限验证完成。


完整工作流34182250412在e5b92ef上全部通过，覆盖当前登记的固定与真实上游线路；PR34182244567亦全部通过。该版本包含证书总有效期/身份校验、Node检查身份前置和Python漏测门禁，早于随后CA位置事务及sudo规则归属改动。

2a33c56的PR34182891077在Ubuntu/Debian的新CA真实init/show/重复拒绝/签发、无证书/错误CA/过期拒绝之后，因VM源码快照白名单遗漏sudo旧夹具而失败（tests/fixtures/legacy/linux/setup_sudo_nopasswd.sh缺失）。这是测试输入缺失；原始被归为product-failure的记录保留，不改写历史JSON。快照现包含所有测试夹具，拒绝源链接，并将快照准备异常纳入环境错误报告；新增测试证明新组件夹具不会被旧白名单遗漏。第22项按跨进程原生及实际CA入口证据结案，当前21项有最终处置、21项未完成。

旧Shell客户端进一步将字段验证前置到目录权限修改和CA请求之前，未知YAML版本拒绝同步和续签，并用作用域EXIT清理临时文件。真实yq v4.53.2、本地HTTP和OpenSSH解析器对照：原c8ffb57把换行写成有效ProxyCommand；此前修复版虽拒绝字段却把0750目录改为0700；当前入口保留0750目录、0640用户配置，不生成片段、不残留下载。仅解析ProxyCommand，未执行或连接注入目标。证据在artifacts/legacy-input/frozen-source；新增VM入口复用同一测试驱动。

Go迁移和配置同步的四种FIFO元数据完整CLI在2a33c56均等待到3秒测试截止；统一普通文件读取后立即拒绝并保留用户状态。小配置仍限4MiB，包含程序备份的操作记录保留原有大文件支持，但也不读取FIFO。原超时证据在artifacts/regression-proof/metadata-fifo-2a33c56/before.log；修复后的SSH Go race通过。一次未清理os导入的编译失败另行保留，没有计作产品通过。


Go同步保留既有托管Include块的位置及原字节，避免改变SSH先出现参数优先的行为。0cce6d9完整CLI加真实ssh -G对照复现用户从preferred-user变为清单用户；修复后重复同步的参数、配置字节和0640权限均保留（artifacts/go/ssh/20260908T034602.881008Z为旧失败，20260908T034717.319700Z为新Go race）。托管块内手改指令在同步、迁移检查、卸载与purge时返回3；迁移把主配置也纳入提交前并发检查。这里只执行测试自建配置的ssh -G，不执行用户Match exec。

Python已有组件不再依赖curl或临时目录工具；仅在需要安装对应组件时检查其依赖。无curl/git/mktemp的PATH对照中，原版连已有组件健康检查也拒绝；新入口正确保留现有工具，并传播损坏工具的退出17。历史失败见artifacts/regression-proof/python-dependencies-0cce6d9；这些组件使用明确的版本命令替身，不冒充真实首次安装验证。

Shell事务通过设备/inode/纳秒ctime复查备份与最终提交阶段的并发变化，覆盖内容不变但ACL/xattr变化的情况，无新增Python/getfattr运行依赖。真实代理入口在候选生成及最终暂存两个位置注入原生xattr改动，旧版均错误提交，新版macOS拒绝并保留用户改动。首次测试误用macOS Python不存在的os.setxattr，已改为系统xattr命令并保留夹具错误日志。Linux验证接入断网容器；此修复不表示公共回滚的历史ACL/xattr比较、多文件服务事务或检查到rename之间的竞争已全部解决。

新增70条Python用例、make check、SSH Go race及锁定Linux ARM64断网容器均通过；后者记录为artifacts/linux-files/20260908T035142.079439Z，包含真实Linux xattr并发保留。此本地构建使用Go1.27.1，CI按go.mod的1.26系列执行，两者分别记录。


0cce6d9完整运行34184584092的Ubuntu、Debian、OpenWrt常规系统作业通过，sudo新增归属保护与旧客户端输入验证已真实完成。Debian观察日志另保存在artifacts/github/34184571603；该PR整体因新提交取消Ubuntu而不算通过。完整线路仍需等待后续overlay及上游步骤。

添加Host的Shell入口也修复了重跑移动Include的问题，保留原位置、用户优先级和无换行文件尾；在写片段前拒绝托管块内手改。ef6f48b旧失败见artifacts/regression-proof/host-include-ef6f48b，新完整入口及真实ssh -G检查通过。另补sudo确认期间的并发编辑检查，删除旧规则前和候选替换前重新确认归属与文件版本；新旧对照加入真实VM，当前尚未取得该新增场景运行结果。

Go新增tests/go-cases.json，按Go语法树枚举顶层测试与fuzz目标，考虑原生构建约束；quality核对声明，运行器核对应执行用例是否全部产生结果。新增测试包会要求扩展清单支持，不能悄悄遗漏。真实临时Go模块的已知成功及“Go退出0但必测声明被删”反向检查通过，后者驱动/JUnit/JSON均失败。子用例及fuzz seed继续用运行事件报告，不将顶层清单当作所有动态组合已覆盖。72条Python、HUD race、SSH目标用例和15秒fuzz验证通过。


候选产物ci.59来自PR34185029014的合并测试提交c4ca3fad（分支头ef6f48b），新增归档迁移/回滚驱动在macOS ARM64及断网Linux ARM64容器通过。实际归档程序替换c8ffb57已登记旧程序，独立手写SSH样本验证优先级、重跑不新增日志、后改程序拒绝回滚、恢复旧程序与密钥不变。此样本不是旧正式SSH发行包；没有扩大真实登录/服务切换结论。JSON绑定候选归档及驱动摘要，JUnit与摘要记录缺失场景；故意破坏校验的反向执行退出1并记录四项not_run。完整发布门槛仍未满足。

PR34185029014在ef6f48b上全部必需作业通过，包括两平台行为、三套系统与四平台归档/原生任务。后续82f0d06的sudo确认竞争和Go用例清单仍由新运行单独验证，不能借用该绿色结果。


sudo普通重跑现在先验证完整visudo策略，再判断无变化；其他规则损坏时不再仅凭本工具文件相同报告健康。独立坏规则的历史对照加入Linux VM，检查失败不修改本工具规则或撤权。新增确认竞争及完整策略场景等待当前提交系统作业，未按语法检查结案。


Shell代理默认启用偏好改为从加锁后的候选快照读取，避免准备锁期间的用户on/off修改被旧偏好覆盖。be0965d两个方向均失败的真实入口证据保存在artifacts/regression-proof/proxy-preference-be0965d，修复后通过。Squid普通空输入沿用已有单一数字监听端口；复杂/多监听不自动降级到51938。新增真实daemon自定义端口重跑、无重启、旧密码HTTP及历史重置缺陷对照，待本次VM验证。


完整运行34184584092在0cce6d9上全部通过，包括固定输入重建、两份独立overlay、真实Node/Python/Docker上游及OpenWrt。be0965d的PR34185966922也全部必需作业通过，包含sudo确认竞争、完整策略校验、Go用例完整性及四平台实际归档迁移/回滚。第17项按未知版本拒绝的真实旧新入口证据结案，当前22项有最终处置、20项未完成。

CA初始化改为0700候选目录内生成真实密钥，使用系统link排他发布；拒绝祖先符号链接和生成期间出现的新私钥/公钥。旧版三个完整入口样本均错误成功，记录在artifacts/regression-proof/ca-key-race-be0965d；新版本地通过。公钥发布冲突时保留已发布私钥、候选及阶段记录，要求明确检查，不默默删除或重建；此处尚未承诺自动恢复整对密钥。公钥登记另加入Linux真实SSH首次拒绝、登记登录、重跑、公共回滚和强制命令限制保留场景，等待本次系统验证。


候选工作流增加Ubuntu/Debian实际归档全系统作业：校验当前提交、SHA256SUMS/manifest/BUILD一致性、真实ELF架构及脚本集合，拒绝归档覆盖测试驱动；Shell入口使用归档内已合并公共库的版本，Go使用归档二进制，不另行构建产品。76条本地Python及成功/错误提交、校验、架构、重复/越界条目等输入反向检查通过；真实ci.59归档输入组装通过，尚不算VM全系统验证。新增作业通过candidate-artifacts结果纳入required-checks。变更映射新增assets维度，HUD单独变更不再运行无关SSH/Shell产物系统测试，公共依赖及打包修改仍运行完整相关验证。


90b135d的PR34186766218全部必需作业通过，新增CA候选发布、Squid非默认端口空输入重跑，以及真实普通用户SSH公钥登记/登录/回滚/强制命令限制保留均取得系统证据。

公钥登记进一步修复“公钥仅出现在注释中却被当作已授权”的误判：由OpenSSH解析实际公钥后比较指纹，保留原注释与受限授权。两个旧完整入口失败样本记录在artifacts/regression-proof/access-comment-5b186ad。空授权文件在当前OpenSSH返回255且stderr带CRLF；中文路径还被诊断转义。最初误按exit1处理的中间失败保留，最终通过/dev/stdin输入并仅接受精确的无公钥结果，其他解析错误仍拒绝。macOS及断网Linux真实入口均通过；实际SSH系统样本增加注释中的禁用公钥引用，等待当前提交再次验证。

新增Shell提交后文件版本记录：公共rollback在恢复前检查inode/ctime，拒绝仅修改ACL/xattr等属性的后续编辑。真实代理入口的旧版误覆盖样本见artifacts/regression-proof/rollback-attributes-d132228；新版macOS与断网Linux通过，78条Python回归通过。用户影响：新事务的文件被修改后，即使正文重新改回，也保守返回3并保留备份；正常未修改文件仍可回滚。不为旧记录或rename后、版本记录前的中断补造证据，这些历史恢复路径仍不具备完整属性保护，因此事务整改未结案。

be0965d完整运行34186002955失败：Debian真实上游线路在client.sh故障注入时直接追加正在执行的客户端，触发ETXTBSY；Ubuntu完整线路通过。原始报告product-failure与test.log保存在artifacts/github/34186002955，不改写原失败。断网Linux最小复现确认运行中ELF拒绝直接写入；候选文件加rename可保留旧进程并执行编辑后的文件。VM驱动按此修正，仍保持真实定时任务运行与卸载冲突断言，等待新提交验证。

Squid增加覆盖整次安装/更新的真实flock锁，在依赖安装和交互之前拒绝竞争者，锁inode保留在/run，不用删除锁文件制造双重锁域。普通单进程使用不改变端口、密码或服务策略；竞争运行返回3，锁释放后可重试。VM新增旧入口忽略锁、新入口无文件/备份/服务副作用和释放后重跑场景，尚待新提交系统证据。该锁仅约束本工具并发，多文件持久恢复与外部管理员编辑保护仍未完成。

d132228的PR34187582510全部必需作业通过，首次包含Ubuntu/Debian实际归档全系统生命周期，同时通过四平台原生归档、macOS真实launchd、三套源码系统与两平台行为。该证据不包含随后新增的提交后属性保护、Squid锁和ETXTBSY驱动修正；最新完整运行34187638297仍在进行，不拿PR绿替代完整发布验收。

公钥入口增加默认粘贴、EOF取消、非法授权选项/多行输入、显式导出验证：无效输入不改文件，默认流程不生成/输出私钥，明确导出只读返回指定文件；本地真实OpenSSH通过。Squid在交互前保存配置/认证快照和文件版本，在验证候选后复查，拒绝等待输入期间的管理员编辑；配置候选、备份与失败恢复保留GNU原生扩展属性及原有模式，显式密码轮换也保留既有认证文件属性。断网Linux实际入口的文件/xattr/锁验证通过，解析器和服务在该快速实验中为模拟（artifacts/squid-adapted-current），不计作真实系统通过。新增VM历史覆盖对照、真实代理功能与属性验证待执行；发布中途之后的外部编辑和持久双文件恢复仍是剩余项。

Go续签窗口新增实际证书寿命检查：旧实现根据YAML请求12h计算窗口，对实际约100分钟的健康证书仍请求CA，对约10分钟证书与默认30分钟检查间隔却报告成功。真实ssh-keygen与完整CLI的两条旧失败保存在artifacts/regression-proof/certificate-window-12e420a。修复后按验证过的当前证书起止时间计算窗口并校验间隔；不改现有任务、密钥或请求时长。完整SSH race测试及make check通过（artifacts/go/ssh/20260908T045902.381602Z）；VM新增健康短证书不替换、不合理间隔返回失败、临期证书真实续签及新SSH连接，等待新提交系统结果。

59d54c9的PR34188380394全部必需检查通过，包含真实Squid排他锁、Shell提交后属性保护与修正后的任务故障注入。其完整请求34188430345在pending时被1062037的新请求取代并取消，未执行，不能算通过。

Squid失败恢复增加发布后的文件版本与候选内容复查：失败期间发生外部编辑时返回3，保留当前两份文件、原备份和recovery-conflict记录，不继续恢复文件或切换服务。断网Linux旧/新实际入口已复现旧版误覆盖、新版保留（artifacts/squid-recovery-adapted-current；服务模拟，非系统证据）；VM新增真实停止daemon、双文件已发布后的管理员编辑与失败恢复对照，待当前代码验证。此冲突可能保留新认证文件和停止的服务，必须明确处理；不能宣称已自动恢复。另修复只读checker遗漏直接命名为.lazycat-operation.*的服务记录，旧失败保存在artifacts/regression-proof/checker-hidden-1062037，新版真实入口JSON/无副作用检查通过。

第15、26项按默认公钥流程和Python独立组件选择的代码及入口证据结案，当前24项有最终处置、18项未验收；这不等于各工具完整生命周期或整体发布已验收。Python默认空回答仅检查uv、显式选择仅检查相应组件，其他工具、keyring与项目环境快照不变；该选择测试的组件版本命令明确使用fixture。

检查器支持 --scan-dir <绝对目录>，用于首次CA初始化中断后还未登记的自定义位置；默认仅增加CA默认目录及合法两行位置记录指向的目录，不遍历其他机器或整个文件系统。通过FIFO充当候选私钥的测试证明发现状态记录不打开密钥；目录去重并拒绝显式符号链接路径。整对密钥恢复仍需明确处理。

1062037的PR34189159804全部必需作业通过，包括Ubuntu/Debian源码及实际归档系统中的Squid管理员交互期间修改保护、原生属性保留、健康短证书不重签、不合理间隔报错、临期真实续签及新SSH登录。该证据早于随后新增的Squid恢复冲突、CA候选发现与默认Python选择补充测试。

6757acd的PR34190289196及完整运行34190285088均通过，包括登记的固定输入重建、重复overlay、真实上游和实际归档验证。该结果不包含后续Squid持久恢复实现。

Squid新增版本化中断记录及 --recover <操作目录>：提交前保存配置/认证的前后内容与属性指纹、原systemd运行/启用状态；prepared/restoring/recovery-conflict阻止普通重跑。恢复前同时验证两份文件与备份，恢复中断可再次执行；只有文件、监听及原服务状态确认后才标记rolled-back。支持active/inactive与enabled/disabled/enabled-runtime；其他状态先审阅，不自动解除mask。记录位于root私有目录，/etc/squid允许其他身份写入时拒绝建立恢复记录。旧格式缺少恢复信息时明确拒绝自动恢复，不补造原服务状态。

属性指纹使用GNU tar的确定性PAX流与SHA-256，覆盖内容、模式、属主、mtime、ACL及可读取的扩展属性；不包括atime/ctime及文件系统专有标志。新VM驱动包含真实SIGKILL（第一次发布、第二次发布、恢复第一份文件后）、旧版半提交后重跑误报成功、后续属性修改冲突、五种服务状态组合及真实HTTP认证观察，尚待本次CI。首次本地适配驱动在观察镜像尚未构建完时启动失败（exit125，artifacts/squid-recovery-driver/initial-environment-result.*），属于测试初始化失败，不能算产品通过；原始记录保留。

本地断网、只读源码适配验证通过五种中断/服务状态组合，真实执行CLI、flock、SIGKILL、htpasswd及文件/ACL/xattr校验；systemd、Squid parser、监听与HTTP在此轮为模拟，不计完整系统通过。记录：artifacts/squid-recovery-driver/result.json。首次进入驱动后发现跨文件系统rename假设并失败，原日志cross-device-driver-failure.log保留；改用明确的fixture移动后重建环境通过。

旧 Shell 客户端将程序安装/更新和缺失 yq 的 Homebrew 安装收敛到显式 install，普通菜单、sync、renew-certs 和未知命令不再隐式安装。完整入口的旧实现五个子场景失败保存在 artifacts/regression-proof/legacy-install-cfbe632/before.log；修复后新旧安装状态、缺失依赖和未知命令均无安装副作用，5条相关入口测试及完整82条Python测试通过。依赖可见性和brew/curl观察使用明确适配，非真实上游安装证据。现有完整安装不搬路径或换密钥，缺依赖的旧环境需明确修复；远程入口公共库加载及安装器多文件更新恢复仍未结案。

CA增加显式recover-init：新候选记录原名称、公钥指纹及原位置状态；只对缺失目标建立原候选的硬链接，拒绝覆盖其他inode、变化的候选或位置记录，原密钥不重新生成。已恢复记录保留且重复运行不改文件。旧6070ec8完整入口缺少恢复能力的失败记录在artifacts/regression-proof/ca-recovery-6070ec8/before.log；本地原生OpenSSH、完整CLI、SIGKILL覆盖私钥发布、公钥发布、恢复中再次中断、外来公钥和位置修改。初版实现误将OpenSSH输出的公钥注释纳入关联比较而失败，修正为比较算法及key数据；不隐藏首次失败。此处尚未声称Linux CI或断电持久性通过。

cfbe632完整运行34196304378的Debian源码和每周线路失败，原始product-failure报告保留在artifacts/github/34196304378。日志确认交互冲突断言失败：新未完成记录扫描也调用read，旧注入器误在扫描时提前编辑，导致编辑发生在快照之前。驱动改为仅在真实read -p提示触发，不放宽冲突断言。6070ec8尚不包含此修正，不能将其运行当作修复后证据；先前cfbe632 PR34196280227被后续推送取消，也不算通过。

按原审阅问题的实际范围结案第11、33、34项：Host不再使用不兼容的旧块删除器，未知归属旧块明确保留并要求审阅；非HUD组件已进入分层自动门禁；平台支持按原生/QEMU/适配/未验证分开表达。依据6757acd已完成的PR及完整矩阵和既有入口/门禁用例，当前27项有最终处置、15项未验收。此处不等于所有工具生命周期已补齐，完整副作用、恢复、发布及逐机验收仍是独立必需条件。

Zsh补充部分安装检查：已有OMZ目录缺失/空加载文件时，旧入口返回成功并修改.zshrc（artifacts/regression-proof/zsh-partial-1bc0cc7/before.log）；新入口在依赖安装前拒绝并保持文件快照不变，新安装器结束后也检查加载文件存在且非空。不会删除旧OMZ或自动重新安装，不将这一检查称为全部主题/插件健康验证。

旧Shell同步与CA续签解耦：先校验完整配置及CA输入，再提交配置并使用同一份已下载清单尝试续签；CA失败返回1，分别报告配置已更新、续签失败，旧证书保留。真实CLI/本地HTTP加明确的YAML查询和失败SSH适配器证明旧入口阻断配置且下载两次，新入口保存配置且只下载一次。新增VM场景使用锁定真实yq、真实CA私钥暂时不可用、新配置下旧证书登录、恢复后续签和再登录，待新提交验证。

本地另用真实yq、HTTP与完整旧/新入口验证同步解耦和一次清单读取，结果通过（artifacts/regression-proof/legacy-sync-1bc0cc7/real-yq.log）；SSH故障仍为适配，不将它标为真实登录。最新85条Python完整回归及make check通过。6070ec8完整运行34196825252因仍含已确认的提示注入问题而取消，取消不算通过；当前1bc0cc7系统验证不包含后续Zsh残缺安装及旧SSH同步解耦改动。

Go补充当前源码和当前Shell归档的精确归属识别，保留历史摘要白名单；编译进程序的Shell字节仅作比较，不执行。旧版两个归属单元子场景失败保存在artifacts/regression-proof/current-shell-ownership-9419c87/before.log；新完整SSH race通过（artifacts/go/ssh/20260908T072148.967309Z）。本地开发归档在macOS26 ARM64原生通过实际Shell包→Go迁移→原Shell程序回滚、独立SSH参数和密钥不变、手改拒绝；开发包及本机Go1.27.1证据不能替代托管Go1.26及当前提交完整验证。

对1bc0cc7的真实Actions归档（PR合并测试提交6db2c79）重新运行新增产物迁移场景，旧Go在migrate --check明确返回3：installed command ownership is unknown；原失败报告在artifacts/package/20260908T072501.540429Z及artifacts/regression-proof/current-shell-ownership-9419c87/before-package.log。新开发归档同场景通过；不是源码构建样本冒充实际归档测试。1bc0cc7的PR34197521618已完成Ubuntu/Debian源码系统通过，含五种Squid中断/服务状态组合、真实HTTP认证与ACL/xattr恢复；当前后续SSH同步与归属改动尚不在该证据内。

1bc0cc7的PR34197521618全部必需作业通过，包含Ubuntu/Debian源码及实际归档全系统、macOS原生任务、四平台原生产物、OpenWrt和两平台行为；其中PR合并测试提交为6db2c79。此结果包含CA中断恢复与Squid持久恢复，但不包含后续9419c87的旧SSH同步、Zsh残缺安装以及随后新增的当前Shell归属识别。

Zsh移除独立的临时配置/锁/备份实现，复用已有文件事务与GNU原生属性复制；同目录候选通过zsh -n后提交，当前用户编辑及旧锁保守阻止修改。新记录可由检查器发现并显式恢复锁/回滚，保留历史备份，不承诺撤销包安装和插件下载。真实旧/新CLI及SIGKILL证明旧流程没有公共恢复记录，新流程恢复原正文、权限、扩展属性后可再次执行。另将脚本头的重复远程bash命令改为仓库入口及就近说明，避免复制curl失败被空bash掩盖的命令；根README不再保留已经过时的旧SSH日常自安装描述。

公共事务增加发布版本复查供自动恢复使用。Host及Docker恢复替换GNU会丢xattr的cp -p，暂存后再次检查并发修改；没有版本证据或属性被编辑时保留现场。Host旧/新完整入口与原生属性编辑测试通过；Docker断网Linux真实文件/CLI、模拟systemd/daemon的旧/新失败恢复通过（artifacts/docker-recovery-adaptation/result.json），首次适配缺少systemd标记被产品正确拒绝的环境错误日志保留。VM新增真实Docker停止/恢复、管理员属性冲突和已保存相同配置的显式应用观察，尚待新提交CI。

事务锁新增实际Shell身份记录，修复父进程退出而持锁子Shell仍运行时，recover-lock错误放行的问题。macOS系统Bash3.2真实父/子进程及SIGKILL先复现旧恢复器错误返回0，新实现拒绝恢复仍存活的子进程。旧开发版缺少身份格式的遗留锁保守拒绝自动恢复，需审阅；正常完成的操作不受影响。此修复不解决恢复器自身强杀留下的.recovery记录。旧失败证据：artifacts/regression-proof/subshell-lock-cc792ed/before.log。

83939a8的PR34199445002全部必需作业通过，包括新增真实旧SSH同步/续签解耦和当前Shell实际归档迁移回退。完整34199467862的重复Ubuntu/Debian生命周期仍在运行；不将它标为完整通过。cc792ed新增Zsh共享事务和Docker/Host属性恢复，等待自身CI。

旧SSH单文件缺库时的bootstrap下载也改为仅显式install允许；四种普通入口的旧下载副作用已复现并保存到artifacts/regression-proof/legacy-bootstrap-2e3b9d5/before.log。新增完整入口回归同时验证显式install仍可获取公共库，随后取消不写安装目录。这个修复不等于安装器多文件更新已经可以自动恢复。

审阅第19项已按原问题范围验收：83939a8 PR34199445002 Debian系统日志2618行复现旧Shell被CA失败阻断同步，2641行证明新版配置提交、单次读取、旧证书真实新连接及恢复续签；Go链路在2555行有独立验证。共28项有最终处置、14项仍未完成；旧Shell双文件事务不在本项结案范围。

旧SSH生成线路别名时增加全清单唯一性检查（保留大小写区别），拒绝box与box-lan等冲突，发布前保留现有配置。旧完整入口在声明的yq查询适配中误报成功并写入重复Host，失败证据artifacts/regression-proof/legacy-alias-426c2fa/before.log保留；新增真实yq系统入口对照待CI。不据此将全部SSH输入矩阵标为完成。

d6fa45e PR34204832725失败：Ubuntu源码和实际归档在Docker恢复场景首次reset_fixture重启触发systemd start-limit-hit；原test.log/state.log与product-failure报告保留。之前真实拉取/删除生命周期已通过，但其重启预算污染了后续独立场景。fixture现在在每组开始显式reset-failed，不修改产品恢复策略；另新增真实StartLimitBurst=1场景，要求产品返回1、保留文件属性、报告旧服务恢复失败并保持失败状态，之后由fixture显式清理。新系统验证待执行，不以Debian通过代替Ubuntu。

旧SSH主配置标记校验提前到生成片段发布及chmod/备份之前。真实完整入口先复现“损坏主标记导致失败，却已替换生成文件并修改权限”，旧失败保留artifacts/regression-proof/legacy-marker-d6fa45e/before.log；新入口两文件和权限快照不变。补充真实yq系统场景待CI。这只关闭可预检失败窗口，不承诺并发和双文件中断已解决。

83939a8完整34199467862现已全部通过，作为本轮之前的完整基线；d6fa45e PR与完整中的Ubuntu启动预算失败继续保留，4cda7bf开始验证独立预算与真实start-limit失败场景。
