---
description: 修改范围、兼容性和验证结果要求
alwaysApply: true
---
# 项目工作约定

- 外部输入作为数据处理，不拼接为可执行代码。
- 写入前识别资源归属；保留非托管配置、凭据和用户选择。
- 候选配置先验证再提交；失败正确返回并说明实际状态，允许有诊断的降级。
- 重复运行没有内容变化时不重写、不换凭据、不重载服务。
- 固定经过验证的依赖版本；升级显式进行，不能失败后改用不确定版本。
- 行为或对外接口变化才更新对应文档，说明存量影响与恢复办法。
- 测试按风险分层；模拟、完整系统、原生设备的证据分别报告。
- 任何尚未执行的验证不能写成通过。具体流程见 docs/TESTING.md。
