format_version = 1

[bark]
server = "https://api.day.app"
# 推荐使用 codex-hud config set key 隐藏输入，不要将实际 Key 提交到仓库。
key = ""

[hud]
enabled = true
stop = true
title_width = 30 # 允许 12–40
body_max_bytes = 3000 # 正文 JSON 编码字节上限，允许 256–3000；旧 body_width 不再生效

[projects]
# "/Users/you/Projects/NekroAgent" = "NekroAgent"
